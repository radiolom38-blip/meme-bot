# 🚀 Meme Pump Bot - Инструкция по исправлению

## ❌ Найденные ошибки:

1. **Отсутствуют учетные данные Telegram** - токен и chat_id не настроены
2. **Блокировка Flask** - `app.run()` не позволял боту запуститься
3. **Неправильный Procfile** - два процесса вместо одного
4. **Нет переменных окружения** - хардкод токенов в коде

## ✅ Что исправлено:

### 1. bot.py
- ✅ Добавлено чтение переменных окружения
- ✅ Проверка наличия токенов при запуске
- ✅ Улучшенная обработка ошибок API
- ✅ Безопасное извлечение данных из JSON
- ✅ Логирование всех операций
- ✅ Обработка таймаутов

### 2. dashboard.py
- ✅ Бот запускается в отдельном потоке
- ✅ Flask и бот работают одновременно
- ✅ Улучшенный HTML с автообновлением
- ✅ API endpoints для здоровья и данных
- ✅ Поддержка переменной PORT от Railway

### 3. Procfile
- ✅ Только один процесс `web`
- ✅ Запускает dashboard.py, который сам запускает бота

## 📋 Инструкция по развертыванию на Railway:

### Шаг 1: Настройка переменных окружения

В Railway добавьте следующие переменные:

```
TELEGRAM_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
SCAN_INTERVAL=30
```

**Как получить токен:**
1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте `/newbot`
4. Следуйте инструкциям
5. Скопируйте токен

**Как получить CHAT_ID:**
1. Найдите @userinfobot в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш ID

### Шаг 2: Загрузите исправленные файлы

Замените в вашем проекте:
- `bot.py`
- `dashboard.py`
- `Procfile`

### Шаг 3: Разверните проект

1. Сделайте commit изменений
2. Push в ваш репозиторий
3. Railway автоматически пересоберет проект

### Шаг 4: Проверка работы

1. Откройте Logs в Railway
2. Вы должны увидеть:
   ```
   🚀 Starting Meme Pump Bot...
   🤖 Starting meme pump scanner...
   📊 Scan interval: 30s
   ```

3. Откройте URL вашего приложения - должен работать dashboard

## 🔍 Мониторинг

- **Dashboard**: `https://your-app.railway.app/`
- **API**: `https://your-app.railway.app/api/signals`
- **Health**: `https://your-app.railway.app/health`

## 🐛 Troubleshooting

### Ошибка: "TELEGRAM_TOKEN не установлен"
- Проверьте переменные окружения в Railway
- Убедитесь, что имя переменной точно `TELEGRAM_TOKEN`

### Бот не отправляет сообщения
- Убедитесь, что CHAT_ID правильный
- Проверьте, что бот не заблокирован в вашем чате
- Отправьте `/start` боту в Telegram

### Dashboard не открывается
- Проверьте логи Railway на ошибки
- Убедитесь, что процесс `web` запущен
- Проверьте, что PORT установлен Railway автоматически

## 📊 Структура проекта

```
.
├── bot.py           # Основной бот-сканер
├── dashboard.py     # Web-интерфейс + запуск бота
├── ai_model.py      # ML модель
├── Procfile         # Конфигурация Railway
├── requirements.txt # Зависимости
├── data.csv         # Обучающие данные (создается автоматически)
├── signals.csv      # История сигналов (создается автоматически)
└── model.pkl        # Обученная модель (создается автоматически)
```

## 🎯 Как это работает

1. **dashboard.py** запускается Railway как web-процесс
2. **dashboard.py** создает отдельный поток для **bot.py**
3. **bot.py** сканирует DEX каждые 30 секунд
4. При обнаружении сигнала отправляется сообщение в Telegram
5. Данные сохраняются в `signals.csv`
6. Dashboard показывает последние 20 сигналов
7. Модель переобучается после каждого скана

## 🔥 Дополнительные улучшения

Вы можете настроить:
- `SCAN_INTERVAL` - интервал сканирования (в секундах)
- Пороги в bot.py: `score > 70`, `prob > 60`, `liq < 20000`
- Алгоритм расчета score: `vsr*25 + bp*15`
